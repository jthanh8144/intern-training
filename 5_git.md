# Git

Provide a description and a screenshot for each of the following questions.

1. Create a personal repository on GitHub.
2. Create a local source folder with a name of your choice, initialize it, and push the source code to the repository created in step 1.
3. Research and describe the commands related to Git.

- git clone
- git pull
- git fetch
- git branch
- git add
- git status
  - When there are no changes
  - When there are changes that have not yet been added
  - When there are changes that have already been added
- git checkout
  - -- \<file name>
  - -b
- git commit
  - -m
  - --amend
- git log
  - without args
  - --oneline
  - --graph
- git diff
- git push .. / git push ... -f
- git reset --soft/hard HEAD~1
- git stash
- git stash pop
- git stash apply
- git branch -D <ten branch>
- git merge
- git rebase

The following items require only a description, not images

4. The steps involved from starting to code a new feature until its completion and the creation of a Pull Request (PR).
5. A description of how to resolve PR conflicts.
